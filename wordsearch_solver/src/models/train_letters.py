"""
train_letters.py

Purpose:
    Train the letter-classification CNN on cropped cell images.

Responsibilities:
    - Load the training and validation datasets
    - Apply transforms/augmentation
    - Run the training loop
    - Evaluate model accuracy
    - Save trained model checkpoints

Why this file exists:
    This module handles the full model training workflow for teaching the CNN
    how to recognize letters from puzzle cell images.
"""