"""
annotate.py

Purpose:
    Draw the solved word locations onto the original puzzle image or export a
    highlighted solved version.

Responsibilities:
    - Overlay boxes, lines, or highlights on found word paths
    - Label or color solved words if desired
    - Save annotated outputs to outputs/solved_images/ or outputs/solved_pdfs/

Why this file exists:
    After solving the puzzle, users need a readable visual output showing where
    each word was found.
"""